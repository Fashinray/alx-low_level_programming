#include <stdio.h>
#include <stdlib.h>
/**
* main - print if the number is positive, zero. or negtive
*prints
* Return: 0
*/
int main(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
       putchar(ch);
}
putchar('\n');
return(0);
}
