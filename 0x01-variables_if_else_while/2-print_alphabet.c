#include <stdio.h>
/**
* main - print if the number is positive, zero. or negtive
*prints
* Return: Always 0 (success)
*/
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
