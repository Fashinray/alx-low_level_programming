#include <stdio.h>
/**
 * main - print if the number is positive, zero, or negative
 *
 * return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	putchar('\n');
	return(0);
}
