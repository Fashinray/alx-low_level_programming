#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always return 0 (success)
 */

int main(void)
{
	char c;

	int d;

	c = 'a';
	d = 0;
	while
		(d < 10) {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
